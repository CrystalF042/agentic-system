"""技术观察员 v1 —— **只描述，不判断。**

## 这一版被刻意限制成什么样

CEO 冻结的四条边界，写在这里，改代码前先读：

1. **只有事实，没有方向。** 三类词一个都不许出现：方向判断、操作建议、
   以及日线看不见的主体（谁在买）。有一条探针**逐字扫这五个模块的全部源码，
   文档字符串也扫**——完整清单在 `scripts/test_technical.py` 的 `BANNED` 里，
   刻意只写在那一处：写在这里就等于让被禁的词出现在被禁的文件里。
2. **一套价区算法，参数冻结。** pivot 窗口 5、聚类 0.5×ATR20、
   触点间隔 ≥5、最少 2 个触点。改参数**必须同时改版本号**——
   有一条探针把参数哈希和版本号绑在一起，只改参数不改版本会红。
3. **Signal Card 的字段是稳定契约，`null` 不等于 `0`。**
   算不出来就是 `null`，并且**必须**在 `reasons` 里写明为什么算不出来。
   把"数据不够"写成 0，下游看到的是"这只票资金流入为零"——
   那是这个项目一整条主线在防的那种缺陷。
4. **不接任何东西。** 不改关注池、不触发一部、不动闸门、不发 Telegram、
   不打分、不产候选、不影响 CRO/PC。v1 的全部产出就是一张卡片。

## 两条额外的工程约束

**`as_of` 从第一天就是参数。** 不是"今天"，是"截止到哪一天"。
没有这个参数就没法做任何回放验证，而事后补进去要动每一个函数签名。

**`observe()` 是纯函数。** 输入是 (OHLCV 面板, as_of)，输出是卡片。
不看时钟、不联网、不读文件、不改输入。探针会检查这五件事。
纯函数的好处很具体：`observe(df[:t])` 必须逐字段等于
`observe(df, as_of=t)` —— 任何未来函数都会让这条断言红。

## 不用 TA-Lib

ATR / OBV / CMF 都是十行以内的算术，自己写的好处是**每一个边界情况
都是我们自己决定的**：`high == low` 那天 CMF 的分母是 0，
TA-Lib 会给你一个数，而我们要的是知道那天被跳过了几次。
"""
from __future__ import annotations

SCHEMA_VERSION = "signal-card-1.0.0"
"""Signal Card 的字段契约版本。**加字段可以不动，改语义必须升版本。**"""

__all__ = ["SCHEMA_VERSION"]
